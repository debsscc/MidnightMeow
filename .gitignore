# -----------------------------------------------------------------------------
# PADRÃO UNITY (Pastas temporárias e de Build)
# -----------------------------------------------------------------------------
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
/[Mm]emoryCaptures/
/[Rr]ecordings/

# -----------------------------------------------------------------------------
# IDE & ARQUIVOS GERADOS AUTOMATICAMENTE (Visto na sua imagem)
# -----------------------------------------------------------------------------
# Visual Studio / Rider / VSCode
.vs/
.vscode/
.idea/
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db
.vsconfig

# -----------------------------------------------------------------------------
# SISTEMA OPERACIONAL
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# PLASTIC SCM (Lixo gerado pelo controle de versão antigo)
# -----------------------------------------------------------------------------
.plastic/
ignore.conf

# -----------------------------------------------------------------------------
# FILTRO ESTRITO (Para limpar a raiz do projeto)
# -----------------------------------------------------------------------------
# Ignora arquivos soltos na raiz que não sejam essenciais
# (Isso vai ignorar arquivos extras como 'AGENTS.md' se não estiverem na lista de exceção)

# Se você quiser ignorar absolutamente TUDO na raiz exceto o básico, descomente a linha abaixo:
# /*.*

# Mas certifique-se de manter estes (Use ! para não ignorar):
!README.md
!.gitignore
!.gitattributes

# NOTA DO ESPECIALISTA:
# Recomendo FORTEMENTE manter estas duas pastas. 
# Se você realmente quiser removê-las, remova o # da frente das linhas abaixo:
# /[Pp]roject[Ss]ettings/
# /[Pp]ackages/