
# Velocidade do projétil da Cora
- ~~Reduzir um pouco a velocidade, ela está muito alta atualmente~~ **Feito:** `DefaultProjectileStats.asset` — `moveSpeed` 20 → 14.

# Vida de Cora
- ~~Fazer a Cora ter menos vida que o Nixie~~ **Feito:** criados `CoraCoreStats.asset` (maxHealth 5) e `NixieCoreStats.asset` (maxHealth 7); perfis `CoraGameplayProfile` / `NixieGameplayProfile` apontam para cada SO.

# Attack Speed do Nixie
- ~~Aumentar o DPS dele, fazendo atacar mais rápido~~ **Feito:** `NixieMeleeCombatStats.asset` — `attackCooldown` 0.45 → 0.32.
